/**
 * @param {}
 */

//TODO: Angular Material tooltip was removed so if that's desired, add Bootstrap one
const KrossoverTeamLabelIconTemplate = () => `

    {{ label.abbreviation }}
`;

export default KrossoverTeamLabelIconTemplate;
