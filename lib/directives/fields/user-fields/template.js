import FieldsTemplate from '../template';

const className = 'user-fields';
const fields = 'event.userFields';

const UserFieldsTemplate = new FieldsTemplate(className, fields).template;

export default UserFieldsTemplate;
